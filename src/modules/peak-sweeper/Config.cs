using BepInEx.Configuration;

namespace SpeedrunMod.PeakSweeper {
    public class Config {
        public ConfigEntry<bool> autoSweep;
        public ConfigEntry<bool> instantBricks;
        public ConfigEntry<bool> instantIce;
        public ConfigEntry<bool> shrubbery;
        public ConfigEntry<bool> snow;
    }
}
